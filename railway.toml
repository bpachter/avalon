[build]
builder = "nixpacks"

[deploy]
startCommand = "if [ -f server/main.py ]; then uvicorn main:app --app-dir server --host 0.0.0.0 --port $PORT; else uvicorn main:app --host 0.0.0.0 --port $PORT; fi"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
